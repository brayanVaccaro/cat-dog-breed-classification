{
    "optimizer_lr": 0.0001,
    "momentum": 0.9,

    "epochs_t_v": 100,
    "batch_size_t": 40,
    "batch_size_v": 16,
    "test_size": 0.25,

    "early_stopping_patience": 6,
    "early_stopping_min_delta": 0.003,

    "save_dir": "./model_weights"
}